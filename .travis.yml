language: node_js
node_js:
   - "0.10"

env: IS_TRAVIS=true

services:
  - couchdb

before_script:
  - npm install gardener -g
  - npm install garden-core -g

script: 
  - cd builds/generic-anc && make

notifications:
  #webhooks: 
    #urls:
    #  - https://medic.slack.com/services/hooks/travis?token=xcYT8yusfEdSwLskhBxK4Vwj
    #on_success: change
    #on_failure: always
  #email:
  #  recipients:
  #    - milan@medicmobile.org

