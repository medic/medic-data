language: node_js
node_js:
   - "0.10"

services:
  - couchdb

before_script:
  - npm install kanso -g
  - npm install grunt-cli -g
  - npm install gardener -g

script: 
  - export DEMOS_DB=http://localhost:5984/medic
  - make

notifications:
  #webhooks: 
    #urls:
    #  - https://medic.slack.com/services/hooks/travis?token=xcYT8yusfEdSwLskhBxK4Vwj
    #on_success: change
    #on_failure: always
  #email:
  #  recipients:
  #    - milan@medicmobile.org

